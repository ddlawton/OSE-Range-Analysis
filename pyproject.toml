[project]
name = "ose-range-analysis"
version = "0.1.0"
description = "OSE Range Analysis - Statistical analysis and GEE precipitation tools for Senegal"
requires-python = ">=3.9"
dependencies = [
    "earthengine-api>=0.1.384",
    "numpy>=1.24.0",
    "requests>=2.31.0",
]

[project.scripts]
generate-precipitation = "scripts.generate_senegal_precipitation:main"
